{
  "name": "lexware-mcp-server",
  "version": "1.0.0",
  "description": "Privater, lokaler MCP-Connector für Lexware Office: Belege hochladen und suchen. Kein Löschen, kein Ändern.",
  "type": "module",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "node --test test/*.test.mjs",
    "pack": "npm run build && npm prune --omit=dev && mcpb pack . lexware-cango.mcpb && npm install"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
